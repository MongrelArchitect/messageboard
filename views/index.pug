extends layout

block content
  .container
    h1= title
    ul.messages
      each message in messages
        li
          div.text=message.text
          div.info
            div.user=message.user
            div.time=message.added
    a(href='/new')='new message'
